using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;


namespace PageBaseManttoCD40
{
    /// <summary>
    /// Descripción breve de PageBaseMantto
    /// </summary>
    public class PageBaseMantto : System.Web.UI.Page
    {
        public PageBaseMantto()
        {
        }
        /*
        public string GetWindowName()
        {
            if (Session["WindowName"] != null)
                return Session["WindowName"].ToString();

            Session["WindowName"] = Guid.NewGuid().ToString().Replace("-", "");
            return Session["WindowName"].ToString();
        }
         */
    }
}
